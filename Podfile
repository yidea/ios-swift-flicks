# pod install
source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'
use_frameworks!    # Instruct CocoaPods to use dynamic frameworks instead of static libraries for Pods

target 'rottenTomato' do
pod 'AFNetworking', '~> 2.6'
end
